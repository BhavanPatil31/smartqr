rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isStudent() {
      return isSignedIn() && exists(/databases/$(database)/documents/students/$(request.auth.uid));
    }

    function isTeacher() {
      return isSignedIn() && exists(/databases/$(database)/documents/teachers/$(request.auth.uid));
    }
    
    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    function getAdminData() {
      return get(/databases/$(database)/documents/admins/$(request.auth.uid)).data;
    }

    function getTeacherData() {
      return get(/databases/$(database)/documents/teachers/$(request.auth.uid)).data;
    }
    
    function getStudentData() {
      return get(/databases/$(database)/documents/students/$(request.auth.uid)).data;
    }

    // Profile Rules: Users can manage their own profile.
    match /students/{studentId} {
      allow read, write: if isOwner(studentId);
    }
    match /teachers/{teacherId} {
      allow read, write: if isOwner(teacherId);
    }
    match /admins/{adminId} {
      allow read, write: if isOwner(adminId);
    }

    // Class Rules
    match /classes/{classId} {
      // Teachers who own the class can read, update, and delete it.
      allow read, update, delete: if isTeacher() && resource.data.teacherId == request.auth.uid;
      
      // Admins in the same department can read, update, and delete classes.
      allow read, update, delete: if isAdmin() && resource.data.department == getAdminData().department;
      
      // A student can read a class if it matches their department and semester.
      allow get: if isStudent() && resource.data.department == getStudentData().department && resource.data.semester == getStudentData().semester;
      
      // A student can list classes that match their department and semester.
      // This is crucial for the getStudentHistory flow.
      allow list: if isStudent() && request.query.where.department == getStudentData().department && request.query.where.semester == getStudentData().semester;

      // Teachers can create classes.
      allow create: if isTeacher();

      // Attendance Subcollection Rules
      match /attendance/{date}/records/{recordId} {
        // A student can create their own attendance record.
        allow create: if isStudent() && request.resource.data.studentId == request.auth.uid;
        
        // Teachers of the class or admins of the department can read attendance.
        allow read: if (isTeacher() && get(/databases/$(database)/documents/classes/$(classId)).data.teacherId == request.auth.uid) ||
                      (isAdmin() && get(/databases/$(database)/documents/classes/$(classId)).data.department == getAdminData().department);
      }
    }
    
    // Allow collection group query for a student's own records across all classes.
    match /{path=**}/records/{recordId} {
      allow read: if isStudent() && resource.data.studentId == request.auth.uid;
    }
  }
}
